{
	"name": "standard",
	"private": true,
	"version": "0.0.0",
	"scripts": {
		"start": "vite",
		"build": "tsc && vite build",
		"preview": "npm run build && vite preview"
	},
	"devDependencies": {
		"@vitejs/plugin-legacy": "^1.7.1",
		"typescript": "^4.5.4",
		"vite": "^2.8.0"
	}
}
